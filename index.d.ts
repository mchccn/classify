/**
 * Parses a string into a class.
 * Can be called either as a normal function or tagged template literal.
 */
export default function classify(strings: string | TemplateStringsArray, ...values: string[]): any;
